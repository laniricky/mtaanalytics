package com.mtaanimation.growthos.android.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Palette ─────────────────────────────────────────────────────────────
// Charcoal backgrounds with vibrant orange accents — premium dark-mode BI feel
val BrandCharcoal       = Color(0xFF1C1C1E)   // darkest background
val BrandSurface        = Color(0xFF2C2C2E)   // card / surface
val BrandSurfaceVariant = Color(0xFF3A3A3C)   // elevated surface

// Accent — vibrant orange for primary actions and highlights
val BrandOrange         = Color(0xFFFF9500)
val BrandOrangeDim      = Color(0xFFCC7700)

// Secondary accent — shades of gray for secondary info
val BrandGray           = Color(0xFF8E8E93)
val BrandGrayDim        = Color(0xFF636366)

// Status colours
val BrandAhead          = Color(0xFF34C759)   // green — ahead of target
val BrandOnTrack        = Color(0xFF007AFF)   // blue — on track
val BrandBehind         = Color(0xFFFF3B30)   // red  — behind target

// Neutrals
val BrandWhite          = Color(0xFFFFFFFF)
val BrandMuted          = Color(0xFFAEAEB2)
val BrandDivider        = Color(0xFF38383A)
