package com.mtaanimation.growthos.android.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Palette ─────────────────────────────────────────────────────────────
// Deep navy backgrounds with vivid cyan/violet accents — premium dark-mode BI feel
val BrandDeepNavy       = Color(0xFF080D1A)   // darkest background
val BrandSurface        = Color(0xFF0F1828)   // card / surface
val BrandSurfaceVariant = Color(0xFF162236)   // elevated surface

// Accent — electric cyan for primary actions and highlights
val BrandCyan           = Color(0xFF00E5FF)
val BrandCyanDim        = Color(0xFF0097A7)

// Secondary accent — soft violet for charts and secondary info
val BrandViolet         = Color(0xFF7C4DFF)
val BrandVioletDim      = Color(0xFF512DA8)

// Status colours
val BrandAhead          = Color(0xFF00E676)   // green — ahead of target
val BrandOnTrack        = Color(0xFF29B6F6)   // blue — on track
val BrandBehind         = Color(0xFFFF5252)   // red  — behind target

// Neutrals
val BrandWhite          = Color(0xFFE8EDF5)
val BrandMuted          = Color(0xFF5C6B8A)
val BrandDivider        = Color(0xFF1E2D45)
