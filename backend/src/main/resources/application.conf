ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.mtaanimation.growthos.backend.ApplicationKt.module ]
    }
}
jwt {
    domain = "https://mtaanalytics.onrender.com"
    domain = ${?JWT_ISSUER}
    audience = "mtaanalytics-users"
    audience = ${?JWT_AUDIENCE}
    realm = "mtaanimation-growthos"
    secret = "secret-key-change-in-production"
    secret = ${?JWT_SECRET}
}
database {
    url = "jdbc:postgresql://localhost:5432/growthos"
    url = ${?DATABASE_URL}
    user = "mtauser"
    user = ${?DATABASE_USER}
    password = "mtapassword"
    password = ${?DATABASE_PASSWORD}
}
